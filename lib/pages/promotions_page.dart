import 'package:flutter/material.dart';

class PromotionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your promotions page content
    return Scaffold(

      body: Center(
        child: Text('Promotions,,, Page Content'),
      ),
    );
  }
}